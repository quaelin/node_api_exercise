const PetitionCount = require('./PetitionCount');

function Root() {
    return (
        <section>
            <h1>Welcome</h1>
            <PetitionCount />
        </section>
    );
}

module.exports = Root;
